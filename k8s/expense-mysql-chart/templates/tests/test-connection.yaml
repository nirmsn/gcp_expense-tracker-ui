apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "expense-mysql-chart.fullname" . }}-test-connection"
  labels:
    {{- include "expense-mysql-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "expense-mysql-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
