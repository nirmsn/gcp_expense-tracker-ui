apiVersion: v1
kind: Service
metadata:
  name: {{ include "expense-backend-chart.fullname" . }}
  labels:
    {{- include "expense-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "expense-backend-chart.selectorLabels" . | nindent 4 }}
