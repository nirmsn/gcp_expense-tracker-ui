replicaCount: 1

image:
  repository: us-central1-docker.pkg.dev/ivoxa-form-dev/my-repo-dev/expense-api-dev
  tag: "dev-cd1e411ff377b4995f6c85a0b23d996530ba9bb5"
  pullPolicy: Always

imagePullSecrets:
  - name: gcp-secret

# service
service:
  type: ClusterIP
  port: 80
  targetPort: 5000

container:
  port: 5000 
    

livenessProbe: null
readinessProbe: null

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: api-dev.ivoxa.ai
      paths:
        - path: /
          pathType: Prefix

  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod

  tls:
    - hosts:
        - api-dev.ivoxa.ai
      secretName: api-dev-ivoxa-tls

env:
  - name: MYSQL_HOST
    value: "mysql"
  - name: MYSQL_PORT
    value: "3306"
  - name: DB_PORT
    value: "3306"
  - name: MYSQL_USER
    value: "expense_user"
  - name: MYSQL_PASSWORD
    value: "expense_pass"
  - name: MYSQL_DB
    value: "expense_db"
  - name: CORS_ORIGINS
    value: "https://dev.ivoxa.ai"

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"