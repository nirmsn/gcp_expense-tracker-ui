apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "expense-ui-chart.fullname" . }}-test-connection"
  labels:
    {{- include "expense-ui-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "expense-ui-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
