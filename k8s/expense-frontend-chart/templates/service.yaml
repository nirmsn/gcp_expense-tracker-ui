apiVersion: v1
kind: Service
metadata:
  name: {{ include "expense-frontend-chart.fullname" . }}
  labels:
    {{- include "expense-frontend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "expense-frontend-chart.selectorLabels" . | nindent 4 }}
